# custom

Starter template for a fully custom project.

Included files:

- `README.md` with setup notes
- `.gitignore` with common defaults
- `Dockerfile` placeholder that already exposes port `8080`

Replace the Docker build steps and app source with your own stack.
